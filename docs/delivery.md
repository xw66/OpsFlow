# OpsFlow 第一版交付记录

日期：2026-09-15。当前交付为可开发、演示和复测的企业工单项目，完整需求范围及历史验证记录见 acceptance.md、verification.md；不将本机结果解释为生产容量承诺。

## 本次收尾

- AI建议直接导入已有公开回复框后编辑，复用覆盖确认、未保存离开保护、版本冲突和人工发送流程。移除第二个编辑框，避免工单刷新丢失编辑内容；重复导入同一建议也会触发确认流程。AI区默认折叠，可手动刷新异步结果。
- 统计新查询先清空旧结果，失败不会展示上个区间的数据；展示实际查询区间、SLA和AI分类比例的分子/分母，标明工作量是当前值，日报重新计算期间显示待更新。
- 通知查询失败不会伪装成空列表，标为已读后即时更新列表。
- 验收准备脚本健康探测修正为当前隔离后端8182，避免误判其他实例已启动。

## 当前验证

| 检查 | 结果 | 原始证据 |
|---|---|---|
| 完整后端回归及打包 | 181通过，0失败/错误/跳过 | target/closing-backend.log |
| 前端行为测试 | 66通过 | target/closing-frontend.log |
| 类型检查与生产构建 | 通过 | target/closing-build.log |
| Chromium完整回归 | 9通过，无自动重试 | target/closing-browser-final.log |
| 首轮浏览器 | 8通过、1异步等待超时，保留记录 | target/closing-browser.log |
| 前端部署 | 镜像重建成功；5174首页及最新JS资源200，同源健康检查UP | target/closing-deploy.log；部署后HTTP核验 |

新增AI成功交互场景仅固定模型输出；创建、分配、接单、草稿编辑与实际评论提交走真实隔离后端。验证人工发送前没有评论，离开被取消后草稿保留，版本更新后可继续编辑并仅提交一条公开评论。这项不代表真实模型质量。真实三类模型调用的既有独立成功记录见 verification.md，未重复产生模型调用费用。

首轮AI降级等待20秒时任务仍为PENDING，随后数据库核查待处理队列已清空；保留失败轮，异步观察窗口改为60秒并重新完整执行9项通过。后台默认每2秒处理一个AI任务，历史积压会增加等待时间，不承诺即时完成。

## 运行与复现

- IDEA：JDK25，使用项目Maven Wrapper；启动依赖及配置方法见根目录README.md。
- 完整应用：按README配置本地环境文件后，Docker Compose构建后端及前端；前端同源代理API。
- 浏览器验收：tests/e2e/README.md。独立项目opsflow-acceptance，数据库3318、Redis6391、Kafka9106、后端8182、前端5274。
- 压测和恢复：docs/performance.md、tests/k6/README.md，保留每次数据量、并发、时长和原始结果，不把历史数据称为本轮重测。
- 简历与演示：docs/resume.md。只引用注明环境和测试范围的真实结果。

长期容量、多节点容灾、模型准确率、工作日历和暂停SLA不属于本次已验证能力。SLA按24×7计时，PENDING不暂停；保留部署默认关闭AI，模型配置由本地私有环境提供。不要提交私有凭据、日志中的令牌或target目录。
