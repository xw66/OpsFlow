# 验证记录

## 2026-09-15：最终收尾回归

- 完整后端181项通过并打包；前端66项通过，类型检查/生产构建通过；隔离Chromium9项通过。见target/closing-backend.log、closing-frontend.log、closing-build.log、closing-browser-final.log。
- AI编辑统一复用公开回复框及既有草稿保护；新增固定模型结果、真实工单及评论后端的浏览器成功交互，验证离开保护、版本变化及单次人工发送。未重复调用外部模型。
- 统计清空旧查询、显示分母与实际区间；通知已读即时响应，失败不显示空数据；修正验收健康检查8182端口。
- 首轮浏览器8通过1等待超时保留在target/closing-browser.log；数据库观察AI积压清空后，将异步观察窗口改为60秒，完整重跑9项通过。没有跳过失败用例。
- 本轮交付说明与复现边界见[delivery.md](delivery.md)。较早阶段记录仍按发生时的版本和范围解释。

## 2026-09-15：最终响应式与可访问性复核

- 静态复核全局跳过链接、可见焦点、语义表单标签、按钮/链接语义、状态提示、键盘路径和 `prefers-reduced-motion`；未发现需要新增依赖的缺口。
- 真实手机 Chromium 场景验证无横向溢出、建单和取消流程；桌面配置、通知、统计、AI 页面与团队/工单流程包含在当前8项完整回归中。
- `docker compose config --quiet` 通过；当前 Compose 仍是开发依赖编排，完整应用镜像与同源代理列入下一阶段。

## 2026-09-15：AI辅助真实浏览器验收

- 无外部模型凭据的隔离环境中，客服请求分类建议后，真实 AI 任务进入失败/禁用终态；页面显示失败降级，工单仍可继续使用，摘要和回复建议入口按客服权限显示。
- AI 面板增加有限轮询，提交后能看到当前请求的终态，不会把旧的 WAITING/PENDING 条目当成当前结果。
- 完整 Chromium 回归 8 项通过：管理员、AI、通知、统计、团队协作、四角色工单闭环、并发接单和手机流程。原始日志：`target/ai-browser-full-final-20260915.log`。
- 前端35项测试、类型检查和生产构建通过；未配置真实外部模型，因此本次不宣称分类准确率、摘要质量或回复质量已验证。
- 测试结束后已停止隔离 E2E 应用及 MySQL、Redis、Kafka。

## 2026-09-15：统计看板真实浏览器验收

- 管理员统计页真实验证日期范围查询、六项指标卡片、无样本指标和日报行状态；普通用户访问显示权限提示且不显示统计数据。
- 完整 Chromium 回归 7 项通过：管理员配置、通知、统计、团队协作、四角色工单闭环、并发接单和手机流程。原始日志：`target/statistics-browser-full-final2-20260915.log`。
- 测试结束后已停止隔离 E2E 应用及 MySQL、Redis、Kafka；未清理开发数据卷。

## 2026-09-15：站内通知真实浏览器验收

- 隔离环境中通过真实接口注册用户、创建工单并由管理员分配在线客服；Kafka 事件被通知消费者处理，客服收到对应未读通知。
- 新增通知 Chromium 场景通过：未读角标、通知列表、标为已读、角标归零及工单跳转。
- 完整 Chromium 回归 6 项通过：管理员配置、通知、团队协作、四角色工单闭环、并发接单和手机流程。原始日志：`target/notifications-browser-full-20260915.log`。
- 首次尝试发生在消费者完成首次分区分配之前，未将该次结果计为通过；消费者稳定后重跑成功。测试结束后已停止隔离应用和依赖。

## 2026-09-15：管理员真实浏览器验收

- 在隔离 E2E 环境执行新增管理员配置场景：角色筛选、跨页查找组/分类/SLA、组编辑及分类/SLA 名称关联通过。
- 完整 Chromium 回归 5 项通过：管理员配置、团队人工分配/跨组转派、四角色工单闭环、并发接单、手机流程。原始日志：`target/admin-browser-full-20260915.log`。
- 测试结束后核对 PID 属于隔离 E2E 应用并停止应用，随后停止隔离 MySQL、Redis、Kafka；未清理开发数据卷。
- 本次修正配置页名称查找数据与配置分页旧响应隔离；前端35项测试、类型检查和生产构建仍通过。

## 2026-09-15：管理员配置阶段回归

- Docker 引擎可用；未重置数据卷，使用 `docker compose up -d --wait` 启动现有 MySQL 8.4、Redis 8.0、Kafka 4.3.1，三项健康检查通过。
- 当前工作区执行 `npm.cmd test` 与 `npm.cmd run build`：35 项测试通过，类型检查和生产构建通过。
- 使用项目 JDK 25.0.3 执行 `mvnw.cmd verify -B -ntp`：180 项测试通过，失败、错误、跳过均为0，打包成功。原始日志：`target/final-full-20260915.log`。
- 此次完整回归覆盖管理员用户查询、角色筛选及认证规则；管理员列表不读取 `password_hash`，角色通过批量查询返回。
- 历史检查点曾只有开发依赖；当前 `compose.yaml` 已补充完整应用镜像、前端同源代理和隔离演示准备入口，最终部署证据见阶段10记录。

## 2026-09-15：响应式与可访问性回归

- 检查全局跳过链接、可见焦点、语义按钮/链接、表单标签、状态提示、键盘操作和 `prefers-reduced-motion`；新增通知、统计、AI 页面保持这些基础约束。
- 使用独立 E2E Compose 环境运行 `npm.cmd run e2e`：4项 Chromium 场景通过，覆盖团队协作、四角色工单闭环、并发接单和手机流程；MySQL、Redis、Kafka 健康后执行，结束后已停止应用与依赖。
- 前端构建及35项行为测试此前通过；本阶段未把既有4项场景扩大解释为通知、统计或AI页面专用验收。
- AI真实模型效果、通知/统计/AI专用浏览器场景和跨浏览器验收仍待完成。

## 2026-09-15：AI回复草稿数据流

- AI回复建议支持人工编辑后放入现有公开回复草稿；最终仍由原 `ReplyComposer` 和工单评论接口发送，AI面板不直接发送消息。
- 版本变化、权限变化和主流程失败仍由现有工单详情/回复逻辑处理；AI建议只在当前工单版本上显示和采纳。
- 前端 `npm.cmd run build` 和 `npm.cmd test -- --run` 通过：35项测试通过，类型检查、构建通过。
- AI阶段仍待真实 Chromium 验收、摘要权限边界测试和外部模型效果验证。

## 2026-09-15：AI辅助面板首个增量

- 工单详情新增折叠式 AI 辅助面板，支持分类、摘要、回复建议请求，显示等待/失败/成功状态和建议版本；分类及回复可人工采纳，采纳请求继续携带工单版本。
- 未配置外部模型时保留失败降级，不将请求成功冒充模型效果；现有后端结构化输出校验和幂等测试继续作为接口依据。
- 前端 `npm.cmd run build` 和 `npm.cmd test -- --run` 通过：35项测试通过，类型检查、构建通过。
- 回复建议的“编辑后发送”还需与现有回复编辑器建立明确的数据流；AI真实模型效果和浏览器流程仍待验收。

## 2026-09-15：统计看板首个增量

- 新增组长/管理员统计入口和页面，接入现有 overview/daily 接口；支持上海时区日期范围、核心指标卡片、每日新增、统计处理中和暂无数据状态。
- 页面展示新增工单、平均响应、平均解决、SLA达成率、超时工单和AI分类采纳率，保留分母为空时的暂无数据语义。
- 前端 `npm.cmd run build` 和 `npm.cmd test -- --run` 通过：35项测试通过，类型检查、构建通过。
- JDK25.0.3 下 `StatisticsIntegrationTest` 通过，真实 Testcontainers MySQL8.4 验证权限、日期边界、空数据、SLA/AI统计口径和日报刷新状态。
- 统计页面真实浏览器验收和通知浏览器验收仍待执行；AI辅助面板随后处理。

## 2026-09-15：站内通知首个增量

- 新增 `GET /api/notifications/unread-count`，保留本人通知查询和本人已读条件更新；通知页支持分页、已读、通知内容及工单跳转。
- 导航新增通知入口；未登录或权限失效时通知列表清空，通知读取失败显示重试状态。
- 前端 `npm.cmd run build` 和 `npm.cmd test -- --run` 通过：35项测试通过，类型检查、构建通过。
- JDK25.0.3 下 `MessagingIntegrationTest` 通过，真实 Testcontainers MySQL8.4/Kafka4.3.1 验证通知幂等、重复投递、已读权限和失败重试。
- 未读角标、管理员配置浏览器验收和通知真实 Chromium 场景仍待完成。

## 2026-09-15：通知未读角标

- 全局导航接入未读数接口，登录后读取；通知标记已读后通过页面事件刷新角标，未认证或读取失败显示为0。
- 前端 `npm.cmd run build` 和 `npm.cmd test -- --run` 通过：35项测试通过，类型检查、构建通过。
- 通知阶段仍需真实 Chromium 流程验证；统计看板在通知验收后开始。

## 2026-09-15：管理员客服成员管理增量

- 管理员页面接入客服组成员列表、成员启停和转组编辑，调用现有成员分页及版本更新接口；角色、组、分类、SLA 操作区继续保留。
- `frontend` 执行 `npm.cmd run build` 和 `npm.cmd test -- --run`：35项通过，类型检查、构建通过。
- 后端配置专项上一轮已在 JDK25.0.3 和 MySQL8.4 Testcontainers 下通过；本轮仅修改前端成员管理，未重复后端专项。
- 管理员模块仍需真实浏览器验收，并补充管理员专用前端行为测试；通过后进入站内通知模块。

## 2026-09-15：管理员配置页面增量

- 管理员页面已接入角色调整和客服成员新增入口，使用现有角色、成员、组状态及版本校验接口；组、分类、SLA 列表使用20条分页并显示关联名称。
- 前端 `npm.cmd run build` 与 `npm.cmd test -- --run` 通过：35项测试通过，类型检查和生产构建通过。
- JDK25.0.3 下 `mvnw.cmd -q -Dtest=SupportIntegrationTest test` 通过，真实 Testcontainers MySQL8.4 执行迁移和配置权限/冲突/禁用规则。
- 管理员配置模块仍未完成最终验收：成员编辑/分页展示、角色与成员真实浏览器流程、完整管理员回归仍待执行。

## 2026-09-15：管理员用户查询基线修复

- 管理员查询增加角色筛选；列表 SQL 仅读取安全用户字段，密码摘要未进入管理员列表投影；角色改为批量读取。
- 前端管理员列表的 offset 变化会重新查询；请求取消、序号隔离、权限失效清空和启停提交中保护已接入，启停原因由管理员提交。
- `frontend` 执行 `npm.cmd test -- --run`：35项通过；`npm.cmd run build`：类型检查和生产构建通过。
- 项目根目录执行 `mvnw.cmd -q -DskipTests compile` 通过；`mvnw.cmd -q -Dtest=AuthIntegrationTest test`：10项通过，Docker/Testcontainers 使用 MySQL 8.4 成功初始化。
- 本次认证专项未重置 Docker 或数据卷。测试输出保留在 Maven Surefire 产物中；完整浏览器复测及管理员角色/组/分类/SLA配置仍未完成。

## 2026-09-15：JDK25完整回归与团队浏览器复测

- 使用 `D:/Environment Variable/scoop/apps/temurin-lts-jdk/current` 执行 `mvnw.cmd verify -B -ntp`：180项通过，0失败、0错误、0跳过，并成功打包。原始输出为本次 `target` 构建产物。
- `tests/e2e/prepare.ps1` 启动独立 `opsflow-e2e` 环境，MySQL、Redis、Kafka 健康，后端8082就绪；未重置数据卷。
- `frontend` 执行 `npm.cmd run e2e`：4项 Chromium 场景通过，覆盖团队人工分配/受限跨组转派、四角色闭环、并发接单和手机注册建单取消；结果写入 `target/e2e/browser-results.json`。
- 第4模块真实浏览器复测通过。管理员角色编辑、组/成员、分类和 SLA 配置仍待实现与验收。

## 2026-09-14：第4模块团队接口检查点（模块未完成）

- 新增团队视图的组、待人工分配、活动升级队列筛选，直接在授权SQL范围内执行；管理员可用MY_GROUP查询全部组，普通用户被拒绝，组长在其他组提交的工单不因此进入其团队队列。
- 新增组成员负载分页接口，包含姓名、在线及有效资格、活动量和纯处理中量；保留零负载和停用成员，只计算当前组数据。
- JDK25下运行`mvnw.cmd test -Dtest=TicketWorkspaceIntegrationTest`，22:17:34完成：10项通过，0失败、0错误、0跳过。实际Testcontainers MySQL8.4执行V1–V9迁移与HTTP权限/分页/SQL断言；日志`target/team-workload-test-20260914.log`。先前仅队列9项检查也通过。
- 尚未接入团队页面，未运行本轮完整后端回归或浏览器验收；不以专项结果替代模块完成。下一步为团队页面、成员负载展示、相关前端测试及跨组真实浏览器流程。

## 2026-09-14：用户与客服闭环及首轮真实浏览器验收

- 编辑复用建单表单，固定待提交版本；409/未知结果保留草稿，经核对最新内容后才可继续。仅原分类/优先级不变时沿用原 SLA，允许原规则停用后修改文字内容；改变规则从最初创建时间重算。附件删除限制本人/管理员且工单允许修改，确认具体文件后提交版本，失败不自动重试。
- 历史、分配、交流接口通过联表补当前姓名与组名；保留记录中的操作人 ID。前端历史按需分页加载，权限失效清空数据。共享列表加入服务端 SLA 筛选，详情返回原筛选列表，用户/客服等待状态文案分开。
- `mvnw.cmd verify` 于21:56:24成功：177项，0失败、0错误、0跳过并打包；已有历史/评论/工作流用例增加名称映射断言。日志 `target/frontend-closure-full-20260914.log`。
- 最终前端30项行为测试、类型检查与生产构建通过，日志 `target/frontend-closure-tests-20260914.log`、`target/frontend-closure-build-20260914.log`。
- 新增 Playwright 1.63.0 和 `tests/e2e/prepare.ps1`。独立 Compose 项目 opsflow-e2e、独立真实 MySQL/Redis/Kafka、后端8082、Vite5174。自动分配/接口限流关闭以稳定手工流程，其对应业务已有后端真实依赖测试；SLA、Outbox、消息消费者保留，外部AI关闭。不是使用接口Mock完成浏览器流程。
- 最终3个Chromium场景全部通过：四角色创建→编辑→上传/删除附件→分配→接单→内部备注→等待补充→恢复→解决→关闭→组长重开；两个真实窗口并发接单得到200/409且历史恰好一次；手机注册、建单、取消，手机/900px平板可见SLA。未配置自动重试。原始输出 `target/e2e-browser-final-20260914.log`，报告 `target/e2e/browser-results.json`，归档摘要 [frontend-closure-20260914.json](results/frontend-closure-20260914.json)。
- 首轮浏览器1失败2通过：分类下拉框隐式标签包含选项文本，精确名称定位超时；补明确无障碍名称后完整重跑3通过。首轮第三场景执行期间应用热更新，故最终验收仅采用随后从头执行的完整结果；原始失败日志保留 `target/e2e-browser-first-20260914.log`。
- 截图检查修正归档工单仍提示可补充的问题、移除空操作区域、恢复平板服务时限展示。最新[手机详情](results/screenshots/user-ticket-mobile.png)与[组长分配历史](results/screenshots/leader-ticket-history.png)来自真实页面。运行步骤见 [浏览器回归说明](../tests/e2e/README.md)。
- 第3模块验收通过。团队、管理、通知、统计、AI页面、完整可访问性和最终部署/压测仍待后续模块，不能宣称完整产品已交付。

## 2026-09-14：前端权限保护与分配转派

- JDK 25 下执行 `mvnw.cmd verify`，21:43:41 构建成功：177 项后端测试，0 失败、0 错误、0 跳过，可执行 JAR 打包成功。日志 `target/frontend-workflow-full-20260914.log`；按类汇总 `results/tests-workflow-20260914.json`。
- 新增授权分页分配候选接口及详情 manager 标识。专项工作台/工作流16项通过，覆盖角色与工单资源权限、跨组双方管理权限、活动量排序及分页、离线/停用/撤角色排除、候选读取后离线时转派不产生副作用。沿用现有数据库事务、版本条件更新和迁移，无新表。
- 前端执行 `npm.cmd test` 与 `npm.cmd run build`：25 项行为测试通过，类型检查和生产构建成功。原始日志 `target/frontend-workflow-tests-20260914.log`、`target/frontend-workflow-build-20260914.log`。
- 新增检查包括公开/内部草稿隔离、403 清除服务端内容并保留本地草稿、旧请求不能污染新页面、连续附件上传在会话失效后停止、退出/切换账号/路由参数变化保护、候选失效409固定原版本且重新确认、实际管理者才可重开。
- 尚未执行本轮真实浏览器端到端、视觉检查及性能复测；不能将以上组件测试与后端集成测试称为完整产品验收。下一模块为编辑、附件删除、操作历史及真实浏览器回归。

## 2026-09-10：阶段11最终验收

- 最终mvnw verify于11:18:53成功：169项测试、0失败、0错误、0跳过，可执行JAR打包成功。完整输出target/module-11-final.log，按类汇总见results/tests-final.json。
- 最终补充历史分类跨组修改的日报刷新及失败回滚测试；此前修复共享标签死锁。两项修复均有真实MySQL回归覆盖。
- 最新构建实际启动，health=UP，47个OpenAPI路径，含统计接口；SHA-256及观察时间见results/final-smoke.json。当前版本与早先压测版本分别追踪，不混用哈希。
- SLA真实deadline演练得到30条工单双违约及60条唯一事件，扫描有任务批次耗时984ms。Kafka暂停通知消费积压360，恢复后归零且消费数恰好增加360；Redis停机业务回源/幂等成功、登录503，恢复登录200；Kafka停机记录失败重试，恢复后事件SENT并消费。
- 最后数据库快照4171条工单，缺失初始历史/创建事件=0，待发送Outbox=0、OPEN消费异常=0、重复通知=0。AI任务仍有积压，明确记录，未冒充Kafka积压已消除即所有异步任务完成。
- 架构、演示/简历、验收矩阵和实测原始指标已归档；未知外部模型效果、初轮列表2次客户端超时及单机短测边界保留在performance.md。阶段11验收完成不等于宣称生产SLO达成。

## 2026-09-10：阶段11进行中

- 真实HTTP跑通创建至关闭，7条历史；k6 32VU同工单竞争接单得到1成功31冲突，数据库仅1条PROCESSING历史。
- 首次1000建单出现795次失败，InnoDB诊断定位空标签关联DELETE间隙锁与共享标签行锁的死锁。调整锁顺序并跳过新建空关联删除后，原10VU/1000次负载全部成功。
- 新增8线程80次创建并修改的回归，逐单核对Outbox和初始历史。2026-09-10 11:00:33完整测试168项通过，0失败、0错误、0跳过；verify最后打包被运行中JAR占用阻止。停止专用压测进程后11:01:03单独package成功，随后启动独立JAR副本，避免再次占用构建目标。
- 后续k6 30秒建单2630次全成功，列表第二轮43691次全成功，冷详情1000次和热点详情75949次全成功。初始列表2次传输超时仍保留，未证实其根因，不以复测通过抹去失败轮次。
- 该条为阶段11进行中的历史记录；阶段12以后已补充 Kafka/SLA/Redis 故障测量和最终交付证据，勿与当前基线混读。

## 2026-09-10：阶段 10

- JDK25.0.3执行 `mvnw verify -B -ntp` 成功：167项测试、0失败、0错误、0跳过，可执行JAR打包成功。原始输出 `target/module-10-test.log`，结束时间10:52:01（Asia/Shanghai）。
- 新增7项真实MySQL统计测试，V9迁移通过：上海时区跨日边界、空集合、响应/解决均值、取消排除与重开周期、SLA样本、AI成功/采纳分母、当前客服工作量、日报待刷新与真实零值、组长权限及查询范围上限。
- 统计重复消费测试直接调用实际EventProcessor与数据库事务，检查重复eventId不会将完成日报再次置脏或累加；跨组转派重算后旧组减少、新组增加。真实Kafka传输与消费去重继续由完整回归中的MessagingIntegrationTest覆盖，不将直接调用伪称为broker压测。
- 注入聚合SQL失败后旧日报保留，事务回滚后可重试；调度每轮最多处理5个日期，积压可在后续轮次完成。
- 初次专项运行7项中1失败、3错误，根因为agents/groups单参数Mapper没有绑定actor名称。增加明确的@Param后原断言全部通过，未弱化权限或统计断言。
- README补充概览/日报接口、样本口径及刷新延迟，数据库设计补充V9表和索引；未执行性能压测，也没有实际外部模型效果数据。

## 2026-09-10：阶段 09

- 最终 `mvnw verify -B -ntp` 在JDK25.0.3上成功：160项测试、0失败、0错误、0跳过，打包成功。输出 `target/module-09-test.log`，结束时间10:35:03（Asia/Shanghai）。
- V8在真实MySQL8.4迁移成功。新增8项AI业务集成测试覆盖创建事件重复入队、失败/禁用不改变工单、显式分类采纳、草稿隐私与单次发送、过期建议/禁用分类拒绝、并发请求去重、租约回收、事务回滚及输入归属权限。
- 8线程相同工单/版本/类型请求仅生成1条分析及1条请求Outbox。模型调用断言数据库事务未开启；分析完成及调用日志在另一个事务提交。旧租约结果记入日志但不能覆盖新持有者结果。
- 注入Outbox错误验证请求和分析整体回滚；注入采纳记录写入错误验证已经准备写入的公开评论和version同时回滚，恢复重试后仅有1条评论。未确认草稿不在公开交流中，普通用户不能查看摘要/回复或调用日志。
- 新增真实Kafka请求测试：手动摘要请求经Outbox和独立AI消费者激活，重复投递后仅1条调用记录；模型关闭记录DISABLED且工单保持CREATED，不产生用户通知。既有创建事件测试同时检查自动分类任务只生成1条。
- AI业务测试定点模拟模型返回以检验事务和权限；独立6项契约测试使用真实Spring AI客户端、本地HTTP服务验证Schema、超时、重试和Token元数据。没有调用真实外部模型，没有分类准确率、外部推理延迟或实际费用数据。
- 本轮初次测试因Docker引擎未启动出现8项环境错误。Docker日志曾报告遗留运行套接字错误；重新启动后引擎恢复，所有集成测试重跑通过，未重置数据卷，也未成功改动运行目录。
- 分类候选第一版限定前100个有效分类，摘要/回复使用最多50条公开交流且有字符上限；分类采纳只允许CREATED工单。进程在外部调用后、记录前崩溃时仍可能重复调用，不能宣称模型请求exactly-once。

## 2026-09-09：阶段 09 调用层检查点（阶段未完成）

- Spring AI 2.0.1核心模型依赖在JDK25.0.3、Boot4.1.1工程中成功编译。完整 `mvnw verify -B -ntp` 通过151项测试，0失败、0错误、0跳过，成功打包；输出 `target/module-09-foundation-test.log`，结束时间23:36:39（Asia/Shanghai）。
- 新增6项调用层测试，以JDK本地HTTP服务返回明确测试响应，真实Spring AI客户端发起请求；验证三类JSON Schema、system/user消息分离、无工具调用、供应商模型名与Token元数据、有界重试、超时、禁用模式及输入输出边界。
- 首次契约测试发现：单独创建结构化请求选项会覆盖默认超时，改为复制模型选项后加入Schema；EmptyUsage返回空Map和0，改为明确识别EmptyUsage并保留未知Token为null。非法枚举样例原误用了合法URGENT，修正为未定义的UNKNOWN_PRIORITY。
- 当前没有调用外部模型，固定HTTP响应不代表模型准确率、真实Token成本或真实推理延迟。模型失败/非法结构返回FAILED，校验未通过的文本不作为结果返回；未配置模型返回DISABLED。
- 阶段09仍未完成：待开发分析及调用日志持久化、异步Kafka消费、分类人工采纳、回复草稿确认和对应权限/事务集成测试。调用层通过不能替代业务验收。

## 2026-09-09：阶段 08

- JDK25.0.3 执行 `mvnw verify -B -ntp` 成功：145项测试、0失败、0错误、0跳过，生成可执行JAR。原始输出 `target/module-08-test.log`，结束时间23:27:28（Asia/Shanghai）。
- 新增8项集成测试使用真实MySQL8.4和独立Redis8.0容器；V7迁移成功。创建接口缺少/非法幂等键返回400，同键不同请求内容返回409，不同用户可独立使用相同键。
- 8线程绕过Redis占位并发提交同一数据库请求键，最终仅1张工单、1条初始历史、1条Outbox。首次响应快照在工单后续修改后仍保持不变；数据库事务失败不留工单/幂等记录，释放Redis租约后可用原键重试。
- Redis原子租约测试验证相同摘要占位冲突、不同摘要拒绝、真实TTL过期以及旧持有者释放不删除新锁；幂等记录不依赖Redis键长期存在。
- 缓存测试确认两次详情读取只加载一次标签，缓存仍查询当前归属权限；提交后失效、回滚保留、SLA变更失效。转派后手动重放旧缓存，旧责任人仍被拒绝。
- 20线程竞争5次限额，恰好5次放行；TTL过期后恢复。登录第11次请求返回429并包含Retry-After。
- 暂停Redis容器模拟命令超时，详情正常回源、创建仍由MySQL去重、登录返回503及Retry-After；恢复容器后Redis读写和详情恢复。没有用mock结果替代实际Redis故障。
- 原有工单HTTP测试已补充必填Idempotency-Key；其他模块显式关闭Redis缓存/占位/限流，真实Redis测试单独开启，未跳过已有回归。
- 没有执行性能压测；120次/分钟业务限流与10次/分钟登录/注册限流属于配置策略，不是系统吞吐能力。AI、统计与阶段11验收仍待开发。

## 2026-09-09：阶段 07

- JDK25.0.3 执行 `mvnw verify -B -ntp` 成功：137项测试、0失败、0错误、0跳过，生成可执行 JAR。原始输出 `target/module-07-test.log`，结束时间23:17:15（Asia/Shanghai）。
- 新增8项真实 MySQL SLA 集成测试，通过固定 Clock 验证预警窗口边界、deadline 完成达标及超过1微秒违约；重复扫描仅一条同类型事件，SLA 不伪造业务状态历史。
- 验证关闭前的超时完成不依赖后台扫描即可记入违约；重开保留首次响应、累计违约，并在新的解决周期中生成独立事件。PENDING 不暂停解决计时，按时完成和取消工单不再生成超时事件。
- 两个独立扫描对象以每种时限单批2条遍历7个测试工单，验证游标分页与重复扫描；7路扫描与1路解决竞争同单，最终仅有一条解决违约，版本冲突后读取最新版本完成处理。
- 模拟Outbox写入失败，核对SLA标记、事件以及完成状态/公开评论整体回滚；故障恢复后重试成功。所有数据库验证使用MySQL8.4，未用H2替代。
- 禁用自动升级仍保存违约，但不提升级别、不通知组长；启用时事件消费产生用户与组长两条通知，同消息重复处理不新增通知。Kafka传输通道继续由阶段06真实broker回归覆盖，本阶段SLA通知断言直接调用同一事务消费者。
- 历史接口沿用工单归属权限，升级队列仅组长/管理员访问且组长限定本组，非法分页返回400。V6已在Testcontainers数据库成功迁移。
- 扫描日志包含实际每轮耗时，但测试样本不作为性能压测或简历数据；EXPLAIN与大数据量压测留在阶段11。Redis、AI及统计尚未开发。

## 2026-09-09：阶段 06

- JDK25.0.3 执行 `mvnw verify -B -ntp` 成功：129项测试、0失败、0错误、0跳过，生成可执行 JAR。原始记录 `target/module-06-test.log`，结束时间23:06:31（Asia/Shanghai）。
- 新增8项集成测试使用 Testcontainers MySQL8.4 与 Apache Kafka4.3.1。覆盖真实投递及重复消息、去重记录与通知事务回滚、6线程相同事件并发消费、ACK后未标记SENT的租约恢复、暂停broker期间建单及恢复投递、短暂失败重试、持续失败DLT和权限重放、DLT发送失败时原offset不前移。
- 租约恢复测试真实等待首条通知入库，模拟发送者在ACK后未更新SENT即退出；租约过期后新持有者再次发送，旧持有者不能标记SENT。等待第二条消息offset提交后仍只有1条通知及1条去重记录。
- 故障注入采用真实组件加定点失败：暂停/恢复Kafka容器模拟不可用；MyBatis通知写入暂时抛错验证事务回滚；DLT发送返回失败Future验证消费者位点。没有把模拟结果称为生产进程崩溃或集群容灾演练。
- 初次专项测试存在Mockito对MyBatis接口代理调用真实方法的错误及由此产生的超时；改为恢复代理默认行为，短暂故障结束后通过真实SqlSession继续执行SQL。修复后专项8项及完整129项均通过。
- V5迁移新增租约、消费去重、失败记录和通知。通知已读仅允许本人；普通用户重放返回403，管理员提交原因后重放原eventId，成功消费将失败记录置为RESOLVED。
- 本阶段未实现统计累加、AI调用、SLA扫描和Redis业务；没有执行性能压测。开发Kafka为单节点，以上结果不证明生产高可用。

## 2026-09-09：阶段 05

- 最新完整命令 `mvnw verify -B -ntp` 在 JDK25.0.3 上成功，121项测试通过，0失败、0错误、0跳过，完成可执行 JAR 打包。
- 新增8项真实 MySQL 工作流集成测试：完整分配/接单/挂起/恢复/解决/关闭/重开链路、并发分配与接单、不同工单并发均衡、候选排除与最久未分配顺序、同组/跨组转派与访问权限、Outbox异常回滚分配及最后分配时间、无候选人及游标扫描、非法状态/角色操作。
- 8路同单分配仅1次成功，8路同单接单仅1次成功；历史、分配和事件数量与唯一成功操作一致。12个独立工单并发分配给2名客服，最终各6个 ASSIGNED，证明尚未接单的任务计入负载。该结果是正确性测试，不是吞吐量压测。
- V4 成功迁移，重开增加 slaCycle、重置当前解决周期并保留首次响应和历史。转派不生成虚假的状态变化历史，责任人权限随转派变化。
- 自动任务默认开启，测试环境关闭后台调度以避免干扰；测试直接运行真实 AssignmentJob.scan 验证游标不会被前序无候选工单阻塞。组锁按编号排序，成员归属与上下线写入遵循相同协调顺序。
- 续跑时 Docker 引擎曾停止，首次回归因 Testcontainers 无法初始化而失败（39个环境错误）；启动 Docker Desktop 并确认 Compose 全部 healthy 后重新运行完整回归通过，未跳过集成测试。
- 原始输出：`target/module-05-test.log`。Kafka 投递、消费者去重/DLT、SLA 扫描、Redis、AI、统计和性能压测仍未完成。

## 2026-09-08：阶段 04

- `mvnw verify -B -ntp` 成功，113项测试通过，0失败、0错误、0跳过，生成可执行 JAR；JDK25/Boot4.1.1 保持不变。
- 新增11项真实 MySQL 工单集成测试：创建截止时间/标签/历史/Outbox、Outbox 异常全事务回滚、资源归属及组长范围、编辑版本冲突与 SLA 快照、改优先级保留计时起点、内部备注隔离及首次响应、4线程取消只有1个成功、无规则/禁用分类/非法参数、附件权限/类型/路径/删除回滚、上传数据库失败清理文件、5MB及10个附件限制。
- V3 成功在 Testcontainers MySQL8.4 迁移。创建工单、标签关联、历史、审计和 Outbox 同事务；Outbox 服务禁止在事务外调用。并发取消只生成一条状态变化事件。
- 工单编辑、取消、评论、附件及既有配置更新均同时校验读取版本和数据库条件更新，拒绝客户端猜测未来版本覆盖旧快照。
- 原始输出：`target/module-04-test.log`；附件测试文件只写入本次生成的 `target/ticket-test-attachments/<UUID>` 目录，不使用开发附件目录。
- 当前附件采用单机文件系统：正常回滚和删除提交已测试；进程崩溃或清理失败可能留下孤儿文件，记录错误并需核对数据库引用后清理。多实例部署前需要共享存储。文件头校验不等于恶意文件扫描。
- 尚未验收：持久化接单/分配/转派/解决/关闭/重开、Kafka 投递及消费者幂等、SLA 扫描、Redis、AI、统计与压测。当前并发取消测试不能替代下一阶段的并发接单测试。

## 2026-09-08：阶段 03

- `mvnw verify -B -ntp` 成功，102 项测试通过，0 失败、0 错误、0 跳过；包括阶段 02 的全部回归。
- 新增6项真实 MySQL 集成测试：分类路由/SLA 唯一约束与时限校验、配置乐观锁冲突、组长归属与普通用户越权、自身在线状态及停用限制、禁用父级与子记录停用、角色合法性与分页上限。
- MySQL 8.4 CHECK 约束返回3819，直接写入非法 SLA 时限也被拒绝；测试核对数据库错误码，不依赖 Spring 将该错误归到某个特定异常子类。
- V2 在 Testcontainers 新 MySQL 上成功迁移，配置变更与审计写入同事务。失败/重复操作没有多写审计。
- 阶段 02 打包产物已额外连接 Compose MySQL 做真实 HTTP 冒烟：health=UP，Swagger 包含注册/登录定义，V1 成功迁移到开发库；该临时服务随后停止。
- 原始输出：`target/module-03-test.log`。阶段 03 的 V2 尚未通过常驻开发实例迁移；下次从 IDEA 启动将由 Flyway 自动执行。
- 尚未实现工单 CRUD、并发接单、Outbox 投递、SLA 扫描、Redis 业务、AI 业务或统计压测，不能据此宣称完整平台已完成。

## 2026-09-08：阶段 02

- JDK 25.0.3 / Boot 4.1.1 / MyBatis Starter 4.0.0，`mvnw verify -B -ntp` 成功，96 项测试通过，0 失败、0 错误、0 跳过，并生成可执行 JAR。
- 新增9项认证集成测试，使用 Testcontainers 独立 MySQL 8.4：注册登录、非法注册/越权字段、6线程重复注册只有1个成功、角色插入失败回滚用户、管理员首次初始化幂等、停用/撤权后旧令牌失效、管理员修改他人及禁止修改自身、普通用户越权、失效/伪造/错误 issuer/错误 audience/无过期时间 JWT。
- Flyway 在新测试数据库成功执行 V1，并在后续上下文中成功校验既有迁移。未使用 H2 模拟 MySQL。
- Docker Compose 的 MySQL 8.4、Redis 8.0、Kafka 4.3.1 均启动为 healthy，弥补阶段 01 的容器启动验证缺口；尚不代表 Kafka/Redis 业务集成已完成。
- 本地 `.env` 已生成随机 JWT 密钥且被 Git 忽略，未修改系统 JAVA_HOME；生产配置没有默认 JWT 密钥。
- 原始输出：`target/module-02-test.log`、`target/surefire-reports/`。状态规则的既有78项检查仍包含在总数中，不把测试总数当作独立功能数。

## 2026-09-08：阶段 01

环境：Windows 11、Temurin JDK 25.0.3、Maven Wrapper 3.9.16、Spring Boot 4.1.1。

执行命令：`./mvnw.cmd verify -B -ntp`，运行前仅在当前命令进程设置 JAVA_HOME 为本机 Temurin 25；未修改全局 Java 或 IDEA 设置。

| 检查 | 实际结果 |
|---|---|
| 状态规则 | 78 项通过：49 种普通转换、28 种重开状态/角色组合及输入边界检查 |
| HTTP 与安全 | 5 项通过：服务信息、健康、Swagger/OpenAPI、匿名 401、已认证拒绝 403、禁止写入；部分测试包含多次请求 |
| 异常处理 | 4 项通过：业务冲突、非法 JSON/校验、方法/内容类型错误、异常细节隐藏 |
| 总计 | 87 项，0 失败、0 错误、0 跳过 |
| 打包 | Maven verify 成功生成可执行 Spring Boot JAR |
| Compose | `docker compose --env-file .env.example config --quiet` 成功 |
| 容器运行 | 未验证：Docker Linux 引擎管道不存在，未执行依赖联调 |

原始 Maven 输出位于本地 `target/module-01-test.log`，JUnit XML 位于 `target/surefire-reports/`，均属于构建产物，不提交 Git；可用上述命令重新生成。

本次发现并修正：Boot 4 模块化测试需要 `spring-boot-starter-security-test` 完成安全测试集成，单独使用 `spring-security-test` 时模拟认证未正确注入 MockMvc。修正后完整回归通过。

尚未验证：真实 MySQL 事务、数据库并发接单、Outbox 一致性、Kafka 消费去重和恢复、SLA 扫描、Redis 幂等、AI 输出、JWT/RBAC 业务接口。当前 401/403 测试仅验证基础安全策略。

没有执行压测，没有产生可写入简历的吞吐量、P95、SLA 扫描性能或消费恢复速率数据。

测试启动仍有 Mockito 动态 agent 警告；当前 JDK 25 测试通过。正式测试基线整理时再按所选 Mockito 版本配置显式 agent。默认生成的开发账号未用于认证，HTTP Basic 与表单登录均关闭，阶段 02 将替换为数据库用户认证。

## 2026-09-15：阶段 10 部署验证

- `docker compose config --quiet` 通过；完整应用 Compose 包含 MySQL、Redis、Kafka、后端和前端五个服务。
- 后端与前端镜像均构建成功；默认实例的后端健康检查、前端首页、反向代理健康接口和同源 API 均返回 HTTP 200。
- 独立部署环境使用后端 8082、前端 5174、MySQL 3308、Redis 6381、Kafka 9096；未删除数据卷。
- 部署环境执行完整 Playwright 回归，8项通过：管理员配置、AI降级、通知、统计、团队协作、四角色闭环、并发接单冲突、手机流程。原始输出：`target/deployed-browser-final-20260915.log`。
- 本阶段验证可启动性、代理连通性和当前业务回归，不等同于性能压测；真实外部模型效果仍未验证。

## 2026-09-15：阶段 11 性能与恢复实测

- 使用独立 `opsflow-bench-20260915` 项目、MySQL 8.4/Redis 8/Kafka 4.3.1、JDK25 应用和固定摘要 k6 镜像；应用端口 8091，未复用正式部署环境数据卷。
- 32 VU 并发接单：32次请求中1次成功、31次预期409，业务检查和阈值通过；HTTP等待时间 P95 96.60ms。
- 创建接口 10 VU/30秒：2091次业务成功，吞吐 69.41次/秒，HTTP耗时 P95 189.57ms、P99 226.77ms。
- 列表接口 10 VU/30秒：14365次业务成功，吞吐 478.62次/秒，HTTP耗时 P95 26.54ms、P99 33.49ms。
- Redis 故障恢复：停止独立 Redis 后创建仍返回201，详情返回200，重复幂等创建返回201且复用工单，故障期间登录返回503；Redis恢复后登录返回200，通知消费恢复，验证通过。原始记录：`target/benchmark/fault-redis-0915154813.json`。
- 以上是当前机器、固定并发和30秒窗口的实测，不是生产容量或长期SLO；Kafka故障恢复和SLA长窗口仍需单独执行。

## 2026-09-15：阶段 12 Kafka 与 SLA 恢复验证

- Kafka 故障：独立环境中停止 Kafka，创建工单仍成功；故障期间 Outbox 保持未发送并观察到1次失败重试，恢复后事件唯一消费完成。原始记录：`target/benchmark/fault-kafka-0915155133.json`。
- SLA 长窗口：使用真实一分钟响应/解决时限创建30条工单，等待自然 deadline 后按工单范围核对数据库，得到 `RESPONSE_WARNING=30`、`RESPONSE_BREACHED=30`、`RESOLVE_WARNING=30`、`RESOLVE_BREACHED=30`，并产生270条相关通知；30条工单均保持 CREATED，未被测试脚本伪造状态。
- 首次 Kafka 观察窗口12秒未捕获失败重试，恢复消费仍成功；该次不计为通过，调整为35秒窗口后重跑通过。脚本保留可调等待参数，避免缩短观察窗口造成误判。
- 当前阶段未执行生产集群、多节点故障或长期容量测试；结果仅适用于独立单节点测试环境。

## 2026-09-15：阶段 15 AI 配置边界复核

- 当前 `.env` 未配置 `AI_API_KEY`，部署环境保持 `AI_ENABLED=false`；未执行外部模型调用。
- 后端 `AiModelServiceTest` 与 `AiIntegrationTest` 专项通过；当前部署环境 AI 浏览器降级场景1项通过，失败信息可见且不阻塞工单流程。原始输出：`target/ai-special-20260915.log`、`target/ai-final-browser-20260915.log`。
- 真实模型成功结构化输出、分类/摘要/回复建议人工采纳仍需可用模型配置后验证；当前结果不代表模型质量或准确率。

## 2026-09-15：管理员页当前工作区验证

- 当前前端 `npm.cmd test`：8 files / 61 tests passed（2026-09-15 18:09 本机时间）。包含35项既有测试和26项新增管理员专项；日志：`target/frontend-admin/admin-test-current.log`。
- 当前前端 `npm.cmd run build`：`vue-tsc --noEmit` 与 Vite production build passed；日志：`target/frontend-admin/admin-build-current.log`。当前 TypeScript 配置包含 `e2e/**/*.ts`，因此本轮同时完成 E2E 脚本类型检查。
- 管理员实现：用户角色预填/自身保护/原因、账号启停、组/成员/分类/SLA原生新增编辑、成员调组和停用、自动携带记录版本、统一提交保护；不再有逗号prompt或手填ID/version操作。页面状态与编辑表单分离到 `useAdmin.ts`、`AdminEditor.vue`，分页与接口校验复用 `adminData.ts`。
- 用户/组/分类名称源与当前列表页独立，按100条逐页读取到结束（用户依据hasMore，其他列表依据返回长度），offset最大100000；达到上限仍有更多数据时明确报错并阻止使用不完整选择器。负责人候选按LEADER、成员按AGENT过滤，编辑时保留原有停用或失去角色的关联，允许停用旧记录。表格显示关联名称。
- 26项管理员测试实际覆盖：超过100条查找和正确关联payload；角色预填/切换原因清空/自身保护；每类写入的防重和失败保留；用户条件与分页、成员分页/切组归零；用户/成员/选择器旧响应隔离；账号切换/降级/停用/卸载清理；401以及列表、选择器、成员、写入返回403后的清理；409保留输入、刷新当前记录后再次确认、使用最新版本，以及并发调组后定位成员；名称、分类代码、SLA上下限校验。
- `frontend/e2e/admin.spec.ts` 已适配原生表单，准备角色及启停、组/分类/SLA新增编辑、成员新增/调组/停用、名称关联与取消场景。`frontend/e2e/team.spec.ts` 已补手机真实刷新按钮及队列查询失败/重试恢复场景；503使用浏览器路由注入，不代表后端故障恢复实测。
- 本轮仅完成代码、单元行为测试及构建/类型检查；未部署、未执行当前版本浏览器、真实AI或压测。管理员浏览器验收与手机视觉/键盘复核仍未验证，历史浏览器/性能记录不计入本轮通过。按本次范围收束，不继续进入通知、统计或AI新模块。
- 后端由并行任务提供本轮 `181/181` 通过记录：`target/maven-verify-20260915.log`；后端管理员专项 `17/17`：`target/admin-support-tests-final3-20260915.log`。本前端任务没有修改或重新运行后端。
- 本轮修改前的源文件和文档备份：`target/frontend-admin/astra-backup/`；更早管理员页基线保留在 `target/frontend-admin/AdminPage.vue.before`。

## 2026-09-15：当前部署接手验证

- 当前工作区 `mvnw.cmd verify -B -ntp`：181项通过，0失败/错误/跳过；日志：`target/maven-verify-current-20260915.log`。前端 `npm.cmd test`：8文件、61项通过，`npm.cmd run build` 通过；日志：`target/frontend-admin/admin-test-browser-fix-20260915.log`、`target/frontend-admin/admin-build-browser-fix-20260915.log`。
- 独立部署后端健康检查和前端首页均返回成功；后端JAR哈希为 `6B477910E1303A320A8428D040D1FE3A276A55F61FE931F637F106D7A9AE6113`。前端独立端口覆写使用 `tests/deploy-frontend.compose.yaml`，未重建依赖容器或删除数据卷。
- 当前浏览器复测中，团队工作台场景通过；管理员闭环首次暴露登录限流和选择器等待/定位问题。验收期间临时关闭AI与限流，结束后限流已恢复、AI仍关闭。管理员脚本定位已修正但尚未在该最后版本重跑，故不计通过。日志：`target/e2e/admin-team-deployed-current-20260915.log`、`target/e2e/admin-deployed-current-20260915.log`。

## 2026-09-15：当前隔离浏览器验收

- `tests/e2e/prepare.ps1` 已迁移到独立 `opsflow-acceptance` 项目，MySQL3318、Redis6391、Kafka9106、后端8182、Vite5274；不复用 `opsflow-deploy` 容器或卷。
- 前端 `npm.cmd test` 61项通过，`npm.cmd run build` 通过；管理员原生闭环独立重跑通过。
- 完整 Chromium 回归8项通过：管理员、AI降级、通知、统计、团队协作、四角色闭环、并发接单和手机流程。原始日志：`target/acceptance-browser-full-20260915.log`。
- 本批AI明确为关闭模型下的失败降级验证；真实分类、摘要和回复建议成功链路尚未计入通过。

## 2026-09-15：隔离真实AI成功路径

- 当前 JAR 的完整 Maven Wrapper 回归：181项通过，0失败/错误/跳过；日志：`target/maven-verify-ai-schema-20260915.log`。
- 分类、摘要、回复建议在 `opsflow-acceptance` 各成功一次；调用记录成功、结果结构校验和普通用户读取调用记录被拒绝均通过。原始脚本日志：`target/acceptance-real-ai-schema-20260915.log`。
- 首次回复建议曾被严格校验拒绝，错误码为 `INVALID_OUTPUT`；修复为在提示词中附带既有服务端 JSON Schema，不降低输出校验规则。旧失败记录保留为历史证据，不混入本次成功结论。

## 2026-09-15：当前版本回归短测与部署

- 独立 `opsflow-bench-current` 环境完成16路并发接单、5 VU创建和5 VU列表短测，所有k6阈值通过。并发接单P95为60.43ms；创建645次、P95为127.10ms；列表4610次、P95为14.83ms。仅适用于本机10秒窗口，不代表容量承诺。日志：`target/benchmark-race-current-20260915.log`、`target/benchmark-create-current-20260915.log`、`target/benchmark-list-current-20260915.log`。
- 当前JAR已通过私有快照的只读挂载重建到 `opsflow-deploy` 应用；健康检查通过，AI保持关闭。直接复制JAR因目标为只读挂载失败，未计为部署成功且未触碰数据卷；重建日志：`target/deploy-current-ai-schema-20260915.log`。
